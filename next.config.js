/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    // if you are using 'src' folder, no special configuration needed unless using appDir
};

module.exports = nextConfig;
